alias functionNum R1;
alias currentPID R2;

if(functionNum == 3) then
     multipush(R1,R2);
     functionNum = 8;
     currentPID = [SYSTEM_STATUS_TABLE + 1];
     call MOD_0;
     multipop(R1,R2);
     print R5;
     multipush(R1,R2);
     functionNum = 9;
     currentPID = [SYSTEM_STATUS_TABLE + 1];
     call MOD_0;
     multipop(R1,R2);
     return;
endif;
return; 
